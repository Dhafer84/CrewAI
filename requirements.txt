# Moteur du crew
crewai==1.15.15
crewai-tools==1.15.15
litellm==1.96.2
python-dotenv==1.2.2

# API live (Phase 6)
fastapi==0.115.5
uvicorn[standard]==0.32.1

# SentinelScan — client API GitHub + rapport Excel
# requests n'est pas épinglé : c'est déjà une dépendance de crewai-tools, qui
# impose >=2.33.0. Un pin exact ici rejouerait le conflit à chaque montée de
# version de crewai-tools.
requests>=2.33.0
openpyxl==3.1.5

# Lecture de documents (utile seulement si les documents source
# passent en .docx/.pdf plus tard — la v1 travaille en .md)
# python-docx==1.1.2
# pypdf==5.6.0

# Qualité / audit de dépendances (dev uniquement)
# pip-audit==2.7.3
